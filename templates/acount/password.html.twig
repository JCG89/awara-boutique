{% extends 'base.html.twig' %}

{% block title %}
  Modifier mon mot de passe
{% endblock %}

{% block content %}
  <div class="title">
    <h3 class="text-center">
      Modifier mon mot de passe
    </h3>
  </div>
  <div class="mesdiv">
    <strong class="text-justify">{{ form(form) }}</strong>
    {% if notification %}
      <div class="alert alert-info">
        {{ notification }}
      </div>
    {% endif %}

    <a href="{{ path('acount') }}">Retourner à mon compte</a>
  </div>
{% endblock %}
